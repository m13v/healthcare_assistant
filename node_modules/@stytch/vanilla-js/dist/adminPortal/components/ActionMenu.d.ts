import React from 'react';
import { TableActionCoreProps } from '../shared/components/TableActions';
export interface Action<T> {
    key: string;
    label: string;
    icon: React.ReactNode;
    onClick: (item: T) => void;
    isDangerous?: boolean;
    isVisible?: boolean | ((item: T) => boolean);
}
interface UseActionMenuParams<T extends {
    id: string;
}> {
    actions: readonly Readonly<Action<T>>[];
    idPrefix: string;
}
export declare const useActionMenu: <T extends {
    id: string;
}>({ actions, idPrefix }: UseActionMenuParams<T>) => {
    getItemActionProps: (item: T) => TableActionCoreProps;
};
export {};
